"""BraneSim experiments package.

Packaged experiment entrypoints that can be run on remote compute (AWS EC2)
via the orchestration/aws/ scaffolding.  Each module is a self-contained CLI
that imports only branesim.* APIs.
"""
